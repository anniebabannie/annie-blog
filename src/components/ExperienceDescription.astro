---
const { title, duration, children } = Astro.props;
---
<div class="pb-16 before:content-[' '] before:w-7 before:h-7 before:rounded-full before:bg-teal-600 before:block before:absolute before:-left-4
">
  <h2 class="uppercase font-bold text-2xl mb-3">{title}</h2>
  <time class="block text-md italic mb-10">{duration}</time>
  {children}
</div>